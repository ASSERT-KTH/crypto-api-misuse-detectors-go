
1. `docker compose -f ./vulnerabilities_conservative up --build --remove-orphans`

2. `docker compose down --remove-orphans`

> Recall that gopher needs to be run from its own directory to produce any reports.