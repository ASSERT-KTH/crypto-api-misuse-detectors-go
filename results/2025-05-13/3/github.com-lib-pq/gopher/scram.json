{
	"Package_Name": "github.com/lib/pq/scram",
	"Dangerous_Functions": null,
	"Warning_Functions": null,
	"Parameter_Constrains": {
		"(*Client).Step": [
			{
				"Predicate": "BYTELENGTH",
				"Object": [
					"0::2",
					"16"
				],
				"Reason": "The length of the key should be at least the same as the output length of the hash function, otherwise it will reduce the security of the MAC function. If authentication is not required, the hash function(sha3/sha256/sha512) is sufficient for use\n\u003e\u003e/analysis/repo/scram/scram.go\nsaltPassword (salt []byte, iterCount int)\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t216:\tfunc (c *Client) saltPassword(salt []byte, iterCount int) {\n\u003e\u003e/analysis/repo/scram/scram.go\nstep2 (in []byte) error\n\t153:\tfunc (c *Client) step2(in []byte) error {\n\u003e\u003e/analysis/repo/scram/scram.go\nStep (in []byte) bool\n\t114:\tfunc (c *Client) Step(in []byte) bool {",
				"InferDepth": 3
			},
			{
				"Predicate": "BYTELENGTH",
				"Object": [
					"0::8",
					"16"
				],
				"Reason": "The length of the key should be at least the same as the output length of the hash function, otherwise it will reduce the security of the MAC function. If authentication is not required, the hash function(sha3/sha256/sha512) is sufficient for use\n\u003e\u003e/analysis/repo/scram/scram.go\nserverSignature () []byte\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t252:\tfunc (c *Client) serverSignature() []byte {\n\u003e\u003e/analysis/repo/scram/scram.go\nstep3 (in []byte) error\n\t198:\tfunc (c *Client) step3(in []byte) error {\n\u003e\u003e/analysis/repo/scram/scram.go\nStep (in []byte) bool\n\t114:\tfunc (c *Client) Step(in []byte) bool {",
				"InferDepth": 3
			},
			{
				"Predicate": "RANDOM_BYTES",
				"Object": [
					"0::8"
				],
				"Reason": "HMAC-key should be random. If authentication is not required, the hash function(sha3/sha256/sha512) is sufficient for use.\n\u003e\u003e/analysis/repo/scram/scram.go\nserverSignature () []byte\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t252:\tfunc (c *Client) serverSignature() []byte {\n\u003e\u003e/analysis/repo/scram/scram.go\nstep3 (in []byte) error\n\t198:\tfunc (c *Client) step3(in []byte) error {\n\u003e\u003e/analysis/repo/scram/scram.go\nStep (in []byte) bool\n\t114:\tfunc (c *Client) Step(in []byte) bool {",
				"InferDepth": 3
			},
			{
				"Predicate": "RANDOM_BYTES",
				"Object": [
					"0::2"
				],
				"Reason": "HMAC-key should be random. If authentication is not required, the hash function(sha3/sha256/sha512) is sufficient for use.\n\u003e\u003e/analysis/repo/scram/scram.go\nsaltPassword (salt []byte, iterCount int)\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t216:\tfunc (c *Client) saltPassword(salt []byte, iterCount int) {\n\u003e\u003e/analysis/repo/scram/scram.go\nstep2 (in []byte) error\n\t153:\tfunc (c *Client) step2(in []byte) error {\n\u003e\u003e/analysis/repo/scram/scram.go\nStep (in []byte) bool\n\t114:\tfunc (c *Client) Step(in []byte) bool {",
				"InferDepth": 3
			},
			{
				"Predicate": "SECURE_HMAC_HASH",
				"Object": [
					"0::0"
				],
				"Reason": "Don't use HMAC_MD5, recommand practice is \" HMAC-SHA256 \".\n\u003e\u003e/analysis/repo/scram/scram.go\nserverSignature () []byte\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t252:\tfunc (c *Client) serverSignature() []byte {\n\u003e\u003e/analysis/repo/scram/scram.go\nstep3 (in []byte) error\n\t198:\tfunc (c *Client) step3(in []byte) error {\n\u003e\u003e/analysis/repo/scram/scram.go\nStep (in []byte) bool\n\t114:\tfunc (c *Client) Step(in []byte) bool {",
				"InferDepth": 3
			}
		],
		"(*Client).clientProof": [
			{
				"Predicate": "BYTELENGTH",
				"Object": [
					"0::8",
					"16"
				],
				"Reason": "The length of the key should be at least the same as the output length of the hash function, otherwise it will reduce the security of the MAC function. If authentication is not required, the hash function(sha3/sha256/sha512) is sufficient for use\n\u003e\u003e/analysis/repo/scram/scram.go\nclientProof () []byte\n\t235:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t235:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t234:\tfunc (c *Client) clientProof() []byte {",
				"InferDepth": 1
			},
			{
				"Predicate": "RANDOM_BYTES",
				"Object": [
					"0::8"
				],
				"Reason": "HMAC-key should be random. If authentication is not required, the hash function(sha3/sha256/sha512) is sufficient for use.\n\u003e\u003e/analysis/repo/scram/scram.go\nclientProof () []byte\n\t235:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t235:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t234:\tfunc (c *Client) clientProof() []byte {",
				"InferDepth": 1
			},
			{
				"Predicate": "SECURE_HMAC_HASH",
				"Object": [
					"0::0"
				],
				"Reason": "Don't use HMAC_MD5, recommand practice is \" HMAC-SHA256 \".\n\u003e\u003e/analysis/repo/scram/scram.go\nclientProof () []byte\n\t235:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t235:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t234:\tfunc (c *Client) clientProof() []byte {",
				"InferDepth": 1
			}
		],
		"(*Client).saltPassword": [
			{
				"Predicate": "BYTELENGTH",
				"Object": [
					"0::2",
					"16"
				],
				"Reason": "The length of the key should be at least the same as the output length of the hash function, otherwise it will reduce the security of the MAC function. If authentication is not required, the hash function(sha3/sha256/sha512) is sufficient for use\n\u003e\u003e/analysis/repo/scram/scram.go\nsaltPassword (salt []byte, iterCount int)\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t216:\tfunc (c *Client) saltPassword(salt []byte, iterCount int) {",
				"InferDepth": 1
			},
			{
				"Predicate": "RANDOM_BYTES",
				"Object": [
					"0::2"
				],
				"Reason": "HMAC-key should be random. If authentication is not required, the hash function(sha3/sha256/sha512) is sufficient for use.\n\u003e\u003e/analysis/repo/scram/scram.go\nsaltPassword (salt []byte, iterCount int)\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t216:\tfunc (c *Client) saltPassword(salt []byte, iterCount int) {",
				"InferDepth": 1
			},
			{
				"Predicate": "SECURE_HMAC_HASH",
				"Object": [
					"0::0"
				],
				"Reason": "Don't use HMAC_MD5, recommand practice is \" HMAC-SHA256 \".\n\u003e\u003e/analysis/repo/scram/scram.go\nsaltPassword (salt []byte, iterCount int)\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t216:\tfunc (c *Client) saltPassword(salt []byte, iterCount int) {",
				"InferDepth": 1
			}
		],
		"(*Client).serverSignature": [
			{
				"Predicate": "BYTELENGTH",
				"Object": [
					"0::8",
					"16"
				],
				"Reason": "The length of the key should be at least the same as the output length of the hash function, otherwise it will reduce the security of the MAC function. If authentication is not required, the hash function(sha3/sha256/sha512) is sufficient for use\n\u003e\u003e/analysis/repo/scram/scram.go\nserverSignature () []byte\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t252:\tfunc (c *Client) serverSignature() []byte {",
				"InferDepth": 1
			},
			{
				"Predicate": "RANDOM_BYTES",
				"Object": [
					"0::8"
				],
				"Reason": "HMAC-key should be random. If authentication is not required, the hash function(sha3/sha256/sha512) is sufficient for use.\n\u003e\u003e/analysis/repo/scram/scram.go\nserverSignature () []byte\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t252:\tfunc (c *Client) serverSignature() []byte {",
				"InferDepth": 1
			},
			{
				"Predicate": "SECURE_HMAC_HASH",
				"Object": [
					"0::0"
				],
				"Reason": "Don't use HMAC_MD5, recommand practice is \" HMAC-SHA256 \".\n\u003e\u003e/analysis/repo/scram/scram.go\nserverSignature () []byte\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t252:\tfunc (c *Client) serverSignature() []byte {",
				"InferDepth": 1
			}
		],
		"(*Client).step2": [
			{
				"Predicate": "BYTELENGTH",
				"Object": [
					"0::2",
					"16"
				],
				"Reason": "The length of the key should be at least the same as the output length of the hash function, otherwise it will reduce the security of the MAC function. If authentication is not required, the hash function(sha3/sha256/sha512) is sufficient for use\n\u003e\u003e/analysis/repo/scram/scram.go\nsaltPassword (salt []byte, iterCount int)\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t216:\tfunc (c *Client) saltPassword(salt []byte, iterCount int) {\n\u003e\u003e/analysis/repo/scram/scram.go\nstep2 (in []byte) error\n\t153:\tfunc (c *Client) step2(in []byte) error {",
				"InferDepth": 2
			},
			{
				"Predicate": "RANDOM_BYTES",
				"Object": [
					"0::2"
				],
				"Reason": "HMAC-key should be random. If authentication is not required, the hash function(sha3/sha256/sha512) is sufficient for use.\n\u003e\u003e/analysis/repo/scram/scram.go\nsaltPassword (salt []byte, iterCount int)\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t216:\tfunc (c *Client) saltPassword(salt []byte, iterCount int) {\n\u003e\u003e/analysis/repo/scram/scram.go\nstep2 (in []byte) error\n\t153:\tfunc (c *Client) step2(in []byte) error {",
				"InferDepth": 2
			},
			{
				"Predicate": "SECURE_HMAC_HASH",
				"Object": [
					"0::0"
				],
				"Reason": "Don't use HMAC_MD5, recommand practice is \" HMAC-SHA256 \".\n\u003e\u003e/analysis/repo/scram/scram.go\nsaltPassword (salt []byte, iterCount int)\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t217:\tmac := hmac.New(c.newHash, []byte(c.pass))\n\t216:\tfunc (c *Client) saltPassword(salt []byte, iterCount int) {\n\u003e\u003e/analysis/repo/scram/scram.go\nstep2 (in []byte) error\n\t153:\tfunc (c *Client) step2(in []byte) error {",
				"InferDepth": 2
			}
		],
		"(*Client).step3": [
			{
				"Predicate": "BYTELENGTH",
				"Object": [
					"0::8",
					"16"
				],
				"Reason": "The length of the key should be at least the same as the output length of the hash function, otherwise it will reduce the security of the MAC function. If authentication is not required, the hash function(sha3/sha256/sha512) is sufficient for use\n\u003e\u003e/analysis/repo/scram/scram.go\nserverSignature () []byte\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t252:\tfunc (c *Client) serverSignature() []byte {\n\u003e\u003e/analysis/repo/scram/scram.go\nstep3 (in []byte) error\n\t198:\tfunc (c *Client) step3(in []byte) error {",
				"InferDepth": 2
			},
			{
				"Predicate": "RANDOM_BYTES",
				"Object": [
					"0::8"
				],
				"Reason": "HMAC-key should be random. If authentication is not required, the hash function(sha3/sha256/sha512) is sufficient for use.\n\u003e\u003e/analysis/repo/scram/scram.go\nserverSignature () []byte\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t252:\tfunc (c *Client) serverSignature() []byte {\n\u003e\u003e/analysis/repo/scram/scram.go\nstep3 (in []byte) error\n\t198:\tfunc (c *Client) step3(in []byte) error {",
				"InferDepth": 2
			},
			{
				"Predicate": "SECURE_HMAC_HASH",
				"Object": [
					"0::0"
				],
				"Reason": "Don't use HMAC_MD5, recommand practice is \" HMAC-SHA256 \".\n\u003e\u003e/analysis/repo/scram/scram.go\nserverSignature () []byte\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t253:\tmac := hmac.New(c.newHash, c.saltedPass)\n\t252:\tfunc (c *Client) serverSignature() []byte {\n\u003e\u003e/analysis/repo/scram/scram.go\nstep3 (in []byte) error\n\t198:\tfunc (c *Client) step3(in []byte) error {",
				"InferDepth": 2
			}
		]
	},
	"Struct_Constrains": null
}